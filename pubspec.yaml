name: unify
description: Prueba técnica de herramientas unificadas.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 0.0.1+1

environment:
  sdk: '>=2.19.4 <4.0.0'

dependencies:
  bloc: ^8.1.0
  bloc_concurrency: ^0.2.0
  curved_navigation_bar: ^1.0.6
  drop_shadow: ^0.1.0
  english_words: ^4.0.0
  equatable: ^2.0.7
  flutter:
    sdk: flutter
  flutter_animate: ^4.5.1
  flutter_bloc: ^8.1.6
  flutter_staggered_grid_view: ^0.7.0
  http: ^0.13.0
  path: ^1.9.0
  provider: ^6.0.0
  sleek_circular_slider: ^2.0.1
  sqflite: ^2.3.3+1

dev_dependencies:
  flutter_lints: ^2.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
